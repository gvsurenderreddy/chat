<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<title>Inscription</title>
		<style>
			h1{
				font-family: Arial, sans-serif;
				color: navy;
			}
			*{
				font-family: Arial, sans-serif;
			}
			p.err{
				padding: 5px;
				background-color: firebrick;
				border-color: red;
				border-radius: 2px;
				border-width: 1px;
				color: white;
			}
			label{
				width: 50px;
			}
			p.msg{
				padding: 5px;
				background-color: #31B404;
				border-radius: 4px;
				border: 1px solid #05883B;
				color: white;
			}
		</style>
	</head>
	<body>
		<h1>Inscription</h1>
		
		<% if (typeof(error) != "undefined") { %>
		<p class="err"><%= error %></p>
		<% } %>
		
		<% if (typeof(message) != "undefined") { %>
		<p class="msg"><%= message %></p>
		<% } %>
		
		<form action="/signin" method="post" id="signin_chat">
			<div>
				<label for="login">Login*:&nbsp;</label>
				<input type="text" name="login" id="login" placeholder="Saisir un login..." size="50" autofocus />
			</div>
			<div>
				<label for="password">Mot de passe*:&nbsp;</label>
				<input type="password" name="password" id="password" size="50" />
			</div>
			<input type="submit" value="Envoyer" />
		</form>
	</body>
</html>
