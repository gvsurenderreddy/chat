<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		
		<link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css" rel="stylesheet">
		
		<!--Pulling Awesome Font -->
		<link href="//maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
		
		<title>Authentification</title>
	</head>
	<body>
		<div id="login-overlay" class="modal-dialog">
			<div class="modal-content">
				<div class="modal-header">
					 <h4 class="modal-title" id="myModalLabel">Authentification requise</h4>
				</div>
				
				<% if (typeof(error) != "undefined") { %>
				<div class="modal-header">
					<p class="err"><%= error %></p>
				</div>
				<% } %>
				
				<% if (typeof(message) != "undefined") { %>
				<div class="modal-header">
					<p class="msg"><%= message %></p>
				</div>
				<% } %>
				
				<div class="modal-body">
					<div class="row">
						<div class="col-md-6">
							<div class="well">
								<form action="/login" method="post" id="login_chat">
									<div class="form-group">
										<label for="username" class="control-label">Nom d'utilisateur</label>
										<input type="text" class="form-control" id="login" name="login" value="" required="" title="Please enter you username" placeholder="login ou adresse e-mail"> <span class="help-block"></span>
									</div>
									<div class="form-group">
										<label for="password" class="control-label">Mot de passe</label>
										<input type="password" class="form-control" id="password" name="password" value="" required="" title="Please enter your password"> <span class="help-block"></span>
									</div>
									<% if (typeof(error) != "undefined") { %>
									<div id="loginErrorMsg" class="alert alert-error"><%= error %></div>
									<% } %>
									<!-- <div class="checkbox">
										<label><input type="checkbox" name="remember" id="remember">Remember login</label>
										<p class="help-block">(if this is a private computer)</p>
									</div> -->
									<button type="submit" class="btn btn-success btn-block">Connexion</button>
								</form>
							</div>
						</div>
						<div class="col-md-6">
							<p class="lead">Inscrivez-vous <span class="text-success">gratuitement</span>
							</p>
							<ul class="list-unstyled" style="line-height: 2">
								<li><span class="fa fa-check text-success"></span> Messagerie instantan&eacute;e </li>
								<li><span class="fa fa-check text-success"></span> Serveur en Node.js, socket.io</li>
								<li><span class="fa fa-check text-success"></span> Fonctionne avec la technologie WebSockets</li>
							</ul>
							<p><a href="/signin" class="btn btn-info btn-block">Je m'inscris</a>
							</p>
						</div>
					</div>
				</div>
			</div>
		</div>
	</body>
</html>
